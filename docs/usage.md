```js static
import { Theme, Button } from 'audora-ui';

const MyApplication = () => (
  <Theme>
    <Button
      content="Click"
      onClick={() => alert("Button was clicked!")}
    />
  </Theme>
);
```
